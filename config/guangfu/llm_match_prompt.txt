任务：根据查询和指令，判断文档是否满足查询需求，仅回复“是”或“否”（严格遵循格式，不添加额外内容）。

<核心判断标准>
1. 满足条件（回复“是”）：文档中的段落、表格（必须带<table>标记）或公式（必须带$$标记）包含查询所需的**关键信息**，且该信息能直接或间接回答查询问题（无需额外外部知识补充）；
2. 不满足条件（回复“否”）：
   - 文档未提及查询相关的关键信息；
   - 仅含查询中的关键词，但无实质性回答内容；
   - 表格/公式虽带对应标记，但内容与查询无关；
   - 需依赖外部知识才能推导答案（文档本身未提供核心依据）。

<指令>：给定网页搜索查询，检索能回答该查询的相关段落、带<table>标记的表格、或带$$标记的公式。

<Few-Shot示例>
示例1：
<文档>：组串式逆变器的直流侧电压范围为500V-1500V，适用于分布式光伏电站。
<查询>：组串式逆变器的直流侧电压是多少？
判断结果为（'是'或'否'）：是

示例2：
<文档>：<table><tr><th>设备类型</th><th>直流侧电压范围</th></tr><tr><td>组串式逆变器</td><td>500V-1500V</td></tr></table>
<查询>：组串式逆变器的直流侧电压是多少？
判断结果为（'是'或'否'）：是

示例3：
<文档>：直流侧电压计算公式：$$U_{dc} = sqrt{2} times U_{ac}$$，其中$U_{ac}$为交流侧额定电压。
<查询>：直流侧电压与交流侧电压的关系是什么？
判断结果为（'是'或'否'）：是

示例4：
<文档>：v，  $$mathbf{g}_{mathrm{v}}$$ ——纵坐标值和垂直坡度（查表得出）；
<查询>：组串式逆变器的直流侧电压是多少？
判断结果为（'是'或'否'）：否

示例5：
<文档>：<table><tr><th>参数</th><th>数值</th></tr><tr><td>垂直坡度</td><td>30°</td></tr></table>
<查询>：组串式逆变器的直流侧电压是多少？
判断结果为（'是'或'否'）：否

示例6：
<文档>：组串式逆变器具有效率高、体积小的特点，广泛应用于光伏系统。
<查询>：组串式逆变器的直流侧电压是多少？
判断结果为（'是'或'否'）：否

<待判断内容>
<文档>：{document}
<查询>：{query}

判断结果为（'是'或'否'）：